#include <stdio.h>
#include "utils.h"


int main(int argc, char const *argv[]){
	int sortlist[] = {12, -4, 8, 1, -3, -9, 17, 14};
	int sortlist_length = sizeof(sortlist) / (int)sizeof(sortlist[0]);
	
	printf("[+] Heapsort needs to be finished...\n");

	return 0;
}
